<%= link_to "Back", wine_path(@wine) %>
<h1><%= wine_name(@wine) %></h1>
<ul>
  <% @wine.ratings.each do |rating| %>
    <li><%=  rating.user.name %> - <%= show_stars(rating.stars) %>
     - <%= link_to "Read the review", wine_rating_path(@wine, rating) %></li>
    <% end %>
    </ul>
