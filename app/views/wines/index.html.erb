<h1>Wines!</h1>
<div class="container"  role="main">
  <% @wines.each do |wine| %>
  <h3><%= link_to wine_name(wine), wine_path(wine) %> </h3><br>
  <% end %>
</div>
<div>
<h3> <%= link_to "Add a new wine", new_wine_path %> </h3>
</div>
