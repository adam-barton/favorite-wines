<h1>Wines!</h1>
<div class="container"  role="main">
  <% @wines.each do |wine| %>
  <p><%= link_to wine_name(wine), wine_path(wine) %> </p>
    <!-- <p> <%= wine.label %> <%= wine.grape %> <%= wine.year %> </p> -->
  <% end %>
</div>
